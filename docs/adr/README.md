# Architecture Decision Records

Every behaviour-changing decision in cluide, newest first. The format and the
`Supersedes` / `Amends` relations are defined in
[`.claude/rules/always-on/adr-discipline.md`](../../.claude/rules/always-on/adr-discipline.md).

**Read the relation column before trusting an ADR.** `accepted` does not mean *wholly* current —
an amended ADR keeps that status while some of its decisions have been reversed. Follow its
`Amended by:` link to find which.

| Date | ADR | Decision | Status | Relations |
|---|---|---|---|---|
| 2026-09-13 | [`2026-09-13-adopt-biome-for-formatting-and-linting.md`](./2026-09-13-adopt-biome-for-formatting-and-linting.md) | **Biome formats and lints the tree.** One dev dependency and one `biome.json` where ESLint plus Prettier would be six and two; oxfmt was pre-1.0. `bun run lint` is the gate and `bun run format` the fix. The recommended rules bend where the code keys effects on triggers, rebuilds lists wholesale, patches untyped JSON and asserts where TypeScript cannot narrow; the specs' SVG diagrams are excluded. | accepted | — |
| 2026-09-13 | [`2026-09-13-adopt-commit-driven-releases.md`](./2026-09-13-adopt-commit-driven-releases.md) | **Releases are cut from a release pull request that a bot derives from the Conventional Commits; npm publishing uses trusted publishing.** release-please keeps one `chore: release X.Y.Z` pull request open with the bumped `package.json` and the changelog; merging it tags the version and runs the publish job, which reruns every check, builds, publishes `cluide` over OIDC with provenance, and attaches the tarball. Labels-and-draft and tag-then-publish were rejected for storing the version intent outside the commits. A repository variable keeps npm off until the repository is public. | accepted | — |
| 2026-09-13 | [`2026-09-13-keyboard-and-cursor-after-hands-on-testing.md`](./2026-09-13-keyboard-and-cursor-after-hands-on-testing.md) | **No theme chord; one base rule gives every clickable control the pointer cursor.** `⌘⇧T` is Chrome's reopen-closed-tab and every other `⌘⇧` letter is bound somewhere, so the theme toggle keeps its two paths (footer button, command menu) and no chord. Tailwind v4's preflight leaves buttons at `cursor: default`; a base rule in `globals.css` sets `pointer` on enabled buttons, links, options, switches and `label[for]`. | accepted | — |
| 2026-09-13 | [`2026-09-13-frontend-build-reconciliations.md`](./2026-09-13-frontend-build-reconciliations.md) | **A failed list read gets a visible state; the warnings panel breakpoint is the viewport, not the frame.** A `LoadFailed` centred state with `Retry`, shared by every list screen and the settings document, replaces the permanent skeleton a 4xx/5xx read left behind; a network failure still shows the offline banner instead. The warnings panel's breakpoint is a viewport media query, matching what was built; `settings.md` reworded from "frame width" to "viewport". | accepted | — |
| 2026-09-12 | [`2026-09-12-cross-site-write-guard.md`](./2026-09-12-cross-site-write-guard.md) | **Writes are guarded by `X-Cluide: 1`, a `Host` check and an `Origin` check, not a token.** The custom header forces a preflight the server never answers, so browsers refuse cross-origin writes; `Host` defeats DNS rebinding; the server binds `127.0.0.1` only. A token was rejected for leaking into URLs or reintroducing CSRF via cookies. | accepted | — |
| 2026-09-12 | [`2026-09-12-one-write-primitive.md`](./2026-09-12-one-write-primitive.md) | **Every write goes through one `writeText`.** Conflict detection by SHA-256 of the managed slice (not mtime, not the whole file); backups in `~/.cluide/backups`, last 50 per file; temp file plus `rename`; diff via `git diff --no-index`. A backup failure aborts the write. | accepted | — |
| 2026-09-12 | [`2026-09-12-vite-builds-the-page.md`](./2026-09-12-vite-builds-the-page.md) | **Vite builds the page; Bun serves it.** Bun's own HTML imports would work with less setup but sit off the path the shadcn CLI, Tailwind and every example assume. Dev is Vite on 5173 proxying `/api` to Bun on 8787 with `changeOrigin`; production is Bun serving `dist/`. | accepted | — |
| 2026-09-12 | [`2026-09-12-hybrid-config-model.md`](./2026-09-12-hybrid-config-model.md) | **One generic files resource for text kinds, typed resources only for settings, MCP and plugins.** Files-first would push the MCP merge and plugin join into the browser; resources-first would need a module per text kind. Both route through the one write primitive; every resource takes a scope. | accepted | — |
| 2026-09-12 | [`2026-09-12-adopt-living-specs-and-adrs.md`](./2026-09-12-adopt-living-specs-and-adrs.md) | **Adopt living specs, ADRs after Stable and ephemeral plans.** Living specs in `docs/spec/<slug>/` state the current design as fact and carry `Draft \| Stable` plus `Built \| Partial \| Planned`; ADRs record decisions with real trade-offs once a spec is `Stable`; plans in `docs/plans/` are committed but deleted when the work lands; three always-on rules in `.claude/rules/` enforce the shapes. The single `docs/architecture.md` is decomposed into the `foundation`, `api` and `ui` specs and deleted. | accepted | — |
